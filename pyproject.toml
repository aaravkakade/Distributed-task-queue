[project]
name = "taskqueue"
version = "0.1.0"
description = "Distributed, fault-tolerant task queue on PostgreSQL"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
